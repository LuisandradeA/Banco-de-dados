@{
    ViewData["Title"] = "Home Page";
    @model List<Empregado>
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
</div>

<div>
   @foreach (var item in Model)
   {
       <p>@item.Nome</p>
   }
</div>


