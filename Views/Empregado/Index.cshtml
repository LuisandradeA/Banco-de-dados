@{
    ViewData["Title"] = "Empregado";
    @model List<Empregado>
}

<div class="text-center">
    <h1 class="display-4">Empregados</h1>
</div>

<div>
   <button type="button" class="btn btn-primary" onclick="location.href='@Url.Action("AdicionarEmpregado", "Empregado")'" />Adicionar Empregado</button>
</div>
<br>
<div>
  <table class="table">
        <tr>
          <th>Nome</th>
          <th>Nome do Meio</th>
          <th>Sobrenome</th>
          <th>Codigo</th>
          <th>Data de Nascimento</th>
          <th>Endereco</th>
          <th>Sexo</th>
          <th>Salario</th>
          <th>gerente</th>
          <th>Departamento</th>
          <th>Editar</tr>
          <th>Remover</th>
          <th>Relatório</th>
          
        </tr>
        @foreach (var item in Model)
        {
            <tr id="@item.Codigo">
                <td>@item.Nome </td>
                <td>@item.NomeDoMeio </td>
                <td>@item.SobreNome </td>
                <td>@item.Codigo </td>
                <td>@item.Dtnascimento </td>
                <td>@item.Endereco </td>
                <td>@item.Sexo </td>
                <td>@item.Salario </td>
                <td>@item.Gerente </td>
                <td>@item.Departamento </td>
                <td><button class="btn btn-success" type="button" id="addressSearch"   
          onclick="location.href='@Url.Action("EditarEmpregado", "Empregado")?empregadoCodigo=@item.Codigo'">Editar</button></td> 
                 <td><button class="btn btn-danger" type="button" id="addressSearch"   
          onclick="location.href='@Url.Action("DeletarEmpregadoAction", "Empregado")?empregadoCodigo=@item.Codigo'">Deletar</button></td> 
                <td><button class="btn btn-info" type="button" id="addressSearch"   
          onclick="location.href='@Url.Action("GetEmpregadoProjeto", "Empregado")?empregadoCodigo=@item.Codigo'">Relatório</button></td> 
            </tr>
        }
        
  
  </table>
</div>


